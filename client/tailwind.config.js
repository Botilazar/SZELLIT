module.exports = {
    content: ["./src/**/*.{js,ts,jsx,tsx}"],
    darkMode: "selector", // new v4 syntax for manual toggling
    theme: {
        extend: {
            /* optional theme extensions */
        },
    },
    plugins: [],
};
